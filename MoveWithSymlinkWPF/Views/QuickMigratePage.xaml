<UserControl x:Class="MoveWithSymlinkWPF.Views.QuickMigratePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MoveWithSymlinkWPF.Views"
             xmlns:vm="clr-namespace:MoveWithSymlinkWPF.ViewModels"
             xmlns:converters="clr-namespace:MoveWithSymlinkWPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        
        <Style x:Key="TaskCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        
        <Style x:Key="GroupBoxStyle" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#F8F8F8" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⏩ 一键迁移" FontSize="20" FontWeight="Bold" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="14" Foreground="Gray" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Target Strategy -->
                <Grid Grid.Row="1" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <CheckBox Content="统一目标目录" 
                                  IsChecked="{Binding UseUnifiedTarget}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"/>
                        
                        <TextBox Text="{Binding UnifiedTargetRoot, UpdateSourceTrigger=PropertyChanged}"
                                 Width="300"
                                 IsEnabled="{Binding UseUnifiedTarget}"
                                 VerticalAlignment="Center"
                                 Margin="0,0,5,0"/>
                        
                        <Button Content="浏览..." 
                                Command="{Binding BrowseUnifiedTargetCommand}"
                                IsEnabled="{Binding UseUnifiedTarget}"
                                VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="导出示例配置" 
                                Command="{Binding ExportExampleConfigCommand}"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                                Visibility="{Binding HasConfig, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                        <Button Content="刷新扫描" 
                                Command="{Binding RefreshScanCommand}"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
                        <Button Content="开始一键迁移" 
                                Command="{Binding StartQuickMigrationCommand}"
                                IsEnabled="{Binding IsExecuting, Converter={StaticResource InverseBoolConverter}}"
                                Background="#0078D4" 
                                Foreground="White" 
                                FontWeight="SemiBold"
                                Visibility="{Binding HasPendingTasks, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Button Content="取消" 
                                Command="{Binding CancelExecutionCommand}"
                                Visibility="{Binding IsExecuting, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
        
        <!-- Content Area -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" Margin="10">
            <!-- 未迁移任务 -->
            <TabItem Header="未迁移">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <!-- Empty State -->
                        <Border Background="#F0F0F0" 
                                Padding="40" 
                                CornerRadius="5"
                                Visibility="{Binding HasValidTasks, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="📂" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="无可一键迁移目录" 
                                          FontSize="16" 
                                          FontWeight="SemiBold"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,5"/>
                                <TextBlock Text="请检查配置文件或刷新扫描" 
                                          FontSize="14" 
                                          Foreground="Gray"
                                          HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Task Groups -->
                        <ItemsControl ItemsSource="{Binding PendingTaskGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding GroupName}" Style="{StaticResource GroupBoxStyle}">
                                        <ItemsControl ItemsSource="{Binding Tasks}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource TaskCardStyle}">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            
                                                            <!-- Row 1: Display Name and Status -->
                                                            <Grid Grid.Row="0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                
                                                                <TextBlock Grid.Column="0" 
                                                                          Text="{Binding DisplayName}" 
                                                                          FontWeight="SemiBold"
                                                                          FontSize="14"/>
                                                                
                                                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding StatusMessage}" 
                                                                              FontSize="12" 
                                                                              Foreground="Gray"
                                                                              Margin="0,0,10,0"/>
                                                                </StackPanel>
                                                            </Grid>
                                                            
                                                            <!-- Row 2: Paths -->
                                                            <Grid Grid.Row="1" Margin="0,5,0,0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="60"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                </Grid.RowDefinitions>
                                                                
                                                                <TextBlock Grid.Row="0" Grid.Column="0" Text="源:" FontSize="11" Foreground="Gray"/>
                                                                <TextBlock Grid.Row="0" Grid.Column="1" 
                                                                          Text="{Binding SourcePath}" 
                                                                          FontSize="11" 
                                                                          FontFamily="Consolas"
                                                                          TextTrimming="CharacterEllipsis"/>
                                                                
                                                                <TextBlock Grid.Row="1" Grid.Column="0" Text="目标:" FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                                                                <TextBlock Grid.Row="1" Grid.Column="1" 
                                                                          Text="{Binding TargetPath}" 
                                                                          FontSize="11" 
                                                                          FontFamily="Consolas"
                                                                          TextTrimming="CharacterEllipsis"
                                                                          Margin="0,2,0,0"/>
                                                            </Grid>
                                                            
                                                            <!-- Row 3: Progress Bar (when executing) -->
                                                            <StackPanel Grid.Row="2" Margin="0,5,0,0">
                                                                <ProgressBar Value="{Binding ProgressPercent}" 
                                                                            Maximum="100" 
                                                                            Height="6"
                                                                            Visibility="{Binding Status, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                                <TextBlock Text="{Binding StatusMessage}" 
                                                                          FontSize="10" 
                                                                          Foreground="Gray"
                                                                          Margin="0,2,0,0"/>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- 已迁移任务 -->
            <TabItem Header="已迁移">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <!-- Empty State -->
                        <TextBlock Text="暂无已迁移任务" 
                                  FontSize="16" 
                                  Foreground="Gray"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Margin="0,40,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MigratedTaskGroups.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                        <!-- Task Groups -->
                        <ItemsControl ItemsSource="{Binding MigratedTaskGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <GroupBox Header="{Binding GroupName}" Style="{StaticResource GroupBoxStyle}">
                                        <ItemsControl ItemsSource="{Binding Tasks}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource TaskCardStyle}">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            
                                                            <!-- Row 1: Display Name and Actions -->
                                                            <Grid Grid.Row="0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                
                                                                <TextBlock Grid.Column="0" 
                                                                          Text="{Binding DisplayName}" 
                                                                          FontWeight="SemiBold"
                                                                          FontSize="14"/>
                                                                
                                                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                                    <Button Content="还原" 
                                                                            Command="{Binding DataContext.RestoreTaskCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                            CommandParameter="{Binding}"
                                                                            Padding="8,4"
                                                                            MinWidth="60"
                                                                            Margin="5,0,0,0"/>
                                                                    <Button Content="清理备份" 
                                                                            Command="{Binding DataContext.CleanupBackupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                            CommandParameter="{Binding}"
                                                                            Padding="8,4"
                                                                            MinWidth="60"
                                                                            Margin="5,0,0,0">
                                                                        <Button.Style>
                                                                            <Style TargetType="Button">
                                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding BackupPath, Converter={StaticResource StringToBoolConverter}}" Value="True">
                                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Button.Style>
                                                                    </Button>
                                                                </StackPanel>
                                                            </Grid>
                                                            
                                                            <!-- Row 2: Paths -->
                                                            <Grid Grid.Row="1" Margin="0,5,0,0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="60"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                </Grid.RowDefinitions>
                                                                
                                                                <TextBlock Grid.Row="0" Grid.Column="0" Text="链接:" FontSize="11" Foreground="Gray"/>
                                                                <TextBlock Grid.Row="0" Grid.Column="1" 
                                                                          Text="{Binding SourcePath}" 
                                                                          FontSize="11" 
                                                                          FontFamily="Consolas"
                                                                          TextTrimming="CharacterEllipsis"/>
                                                                
                                                                <TextBlock Grid.Row="1" Grid.Column="0" Text="目标:" FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                                                                <TextBlock Grid.Row="1" Grid.Column="1" 
                                                                          Text="{Binding SymlinkTarget}" 
                                                                          FontSize="11" 
                                                                          FontFamily="Consolas"
                                                                          TextTrimming="CharacterEllipsis"
                                                                          Margin="0,2,0,0"/>
                                                                
                                                                <TextBlock Grid.Row="2" Grid.Column="0" Text="迁移:" FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                                                                <TextBlock Grid.Row="2" Grid.Column="1" 
                                                                          Text="{Binding MigratedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" 
                                                                          FontSize="11"
                                                                          Margin="0,2,0,0"/>
                                                            </Grid>
                                                            
                                                            <!-- Row 3: Status Message -->
                                                            <TextBlock Grid.Row="2" 
                                                                      Text="{Binding StatusMessage}" 
                                                                      FontSize="11" 
                                                                      Foreground="Orange"
                                                                      Margin="0,5,0,0"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </GroupBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
        
        <!-- Log Area -->
        <Border Grid.Row="2" 
                Background="#F8F8F8" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,1,0,0"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="执行日志" FontWeight="SemiBold" Margin="0,0,15,0"/>
                    <TextBlock Text="{Binding CompletedCount, StringFormat='完成: {0}'}" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding FailedCount, StringFormat='失败: {0}'}" Foreground="Red" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding TotalCount, StringFormat='总计: {0}'}" Foreground="Gray"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding LogMessages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" 
                                          FontFamily="Consolas" 
                                          FontSize="11" 
                                          TextWrapping="Wrap"
                                          Margin="0,1"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>

